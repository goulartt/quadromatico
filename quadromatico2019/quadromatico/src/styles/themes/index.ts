import { createMuiTheme } from '@material-ui/core/styles';

export const themeOrangeUnifil = createMuiTheme({
  palette: {
    primary: {
      main: '#FF9522',
      contrastText: '#fff',
    },
  },
});
